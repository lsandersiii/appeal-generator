import React from 'react';
import ReactDOM from 'react-dom/client';
import AppealLetterGenerator from './AppealLetterGenerator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppealLetterGenerator />
  </React.StrictMode>
);
